[profile.default]
src = "src"
out = "out"
libs = ["lib"]
# Required for the 11-field Escrow struct (avoids "stack too deep").
via_ir = true

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
